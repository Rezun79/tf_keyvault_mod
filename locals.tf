locals {
  # key_vault_name = var.key_vault_name
  # secret = var.secret_name
  root_spn_client_id = var.root_spn_client_id
  spn_client_id = var.spn_client_id
  spn_object_id = var.spn_object_id
  secret = var.secret
  platform_rg = var.platform_rg
  platform_keyvault = var.platform_keyvault
  rg_name = var.rg_name
  tenant_id = var.tenant_id
  # key_vault_rg_name = var.key_vault_rg_name
}
